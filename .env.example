# Optional. Leave empty for Expo Router's relative API route fetches in development.
# Set this when the API is hosted separately from the app, for example:
# EXPO_PUBLIC_API_URL=https://api.example.com
EXPO_PUBLIC_API_URL=
